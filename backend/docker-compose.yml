version: "3.8"
services:
  db:
    image: postgres:16
    container_name: calclone_db
    environment:
      POSTGRES_DB: calclone
      POSTGRES_USER: caluser
      POSTGRES_PASSWORD: calpass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
